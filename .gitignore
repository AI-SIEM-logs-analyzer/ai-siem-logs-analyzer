.idea
.DS_Store

# Local environment files (secrets) — commit only *.env.example
.env
*.env
!*.env.example

# Build output and dependencies, wherever a tool decides to put them. The per-area
# .gitignore files cover backend/target and frontend/node_modules; these catch anything
# added later outside them.
node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
