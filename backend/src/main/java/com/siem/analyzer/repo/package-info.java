/**
 * Persistence access.
 *
 * <p>Repositories are the only place that talks to the database. Everything above this package
 * works with {@link com.siem.analyzer.domain} types, not with rows or queries.
 */
package com.siem.analyzer.repo;
