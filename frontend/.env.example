# Copy to .env.local to override. Unset means same origin, which is what the dev proxy expects.
# A backend on another origin must allow this one through CORS.
# VITE_API_BASE_URL=http://localhost:8080
